<% @projects.each do |project| %>
<p>
    <strong>
        <%= project.title%>
    </strong>
    <%= project.description%>
    <%= "| #{project.percent_complete}" if project.percent_complete%>

</p>
<% end %>